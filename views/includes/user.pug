.user
    .no-login
        if !loginUser
            a(href='/login') 登录
            span #[strong  |] 
            a(href='/register') 注册
        else
            a(href='javascript:void(0)') 您好 #[strong #{loginUser.user}]
            span #[strong  |] 
            a(href='/setting' target='_blank') 设置
            span #[strong  |] 
            a.logout(href='javascript:void(0)') 退出
            script(src="/js/logout.js")